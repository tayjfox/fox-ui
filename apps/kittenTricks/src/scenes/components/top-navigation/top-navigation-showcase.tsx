import React from 'react';

import {
  TopNavigation,
  TopNavigationElement,
  TopNavigationProps,
} from '@meow-ui/components';

export const TopNavigationShowcase = (
  props?: TopNavigationProps,
): TopNavigationElement => <TopNavigation {...props} />;

export default TopNavigationShowcase;
