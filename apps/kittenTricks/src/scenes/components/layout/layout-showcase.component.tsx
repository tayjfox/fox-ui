import React from 'react';
import { Layout, LayoutElement, LayoutProps } from '@meow-ui/components';

export const LayoutShowcase = (props?: LayoutProps): LayoutElement => (
  <Layout {...props} />
);
