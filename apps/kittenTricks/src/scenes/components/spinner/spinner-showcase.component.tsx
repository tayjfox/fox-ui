import React from 'react';
import { Spinner, SpinnerElement, SpinnerProps } from '@meow-ui/components';

export const SpinnerShowcase = (props?: SpinnerProps): SpinnerElement => (
  <Spinner {...props} />
);
