import React from 'react';

import { Card, CardElement, CardProps } from '@meow-ui/components';

export const CardShowcase = (props: CardProps): CardElement => (
  <Card {...props} />
);

export default CardShowcase;
